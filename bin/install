#!/usr/bin/env bash

set -euo pipefail

# shellcheck source=../lib/utils.bash
source "$(dirname "$0")/../lib/utils.bash"

function install_firebase() {
    helper
    printf "install your tool here\\n"
}

install_firebase
